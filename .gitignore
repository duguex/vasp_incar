__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
dist/
build/
*.log
.DS_Store
_.DS_Store

# Personal / loose input files (not part of the package)
INCAR.good
input.UCell
input.cif
input.xyz
input_rst/
mlff_tutorial/
vasp_mlff_guide.md

# work/ parsed data is large and only needed for DB rebuild
work/parsed/

# Local engine benchmark runtime (slim reports live under docs/benchmarks/)
work/benchmarks/

# OpenMX runtime dumps (cubes / restarts)
*.cube
*_rst/

# Dead tantivy BM25 index (hybrid search uses FTS5 + semantic/tag now)
vasp_query/data/search_index/

# Regenerable OpenMX manual DB (rebuild: scripts/build_openmx_db.py)
openmx.db
